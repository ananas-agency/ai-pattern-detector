# ai-pattern-detector v1.6.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add register-awareness, a lexical-diversity metric, a nominalization tell, and a worked-examples library to the ai-pattern-detector skill, plus two new academic sources and clean packaging — shipping as v1.6.0.

**Architecture:** This is a prose/instruction skill, not code. "Implementation" means editing Markdown instruction files (`SKILL.md`, `references/patterns.md`), creating one new reference file (`references/examples.md`), and updating packaging (`plugin.json`, `CHANGELOG.md`, `README.md`). There is no test runner — each task's verification is a concrete check: re-read the edited section, grep for consistency, and (where useful) mentally run a sample scan against the new instruction. The detection engine, severity scoring, and AI-feel score formula are unchanged; register and lexical diversity calibrate *what counts as a tell*, never the score.

**Tech Stack:** Markdown. Git. No build, no dependencies.

## Global Constraints

- **Target version:** `plugin.json` `version` becomes exactly `1.6.0` (from `1.5.0`).
- **Do not change the AI-feel score formula or severity weights.** `score = min(100, Strong × 12 + Medium × 5 + Weak × 2)` stays as-is.
- **Pattern-category count stays 11.** The nominalization tell is a new bullet *inside* §9, not a 12th category. Wording like "eleven categories" in `SKILL.md` and `README.md` stays accurate.
- **Register calibrates what counts as a tell, never the score.** Every register mention must preserve this.
- **The seven registers, named exactly:** Social post · Marketing / landing page · Product description / e-commerce · Blog / editorial / newsletter · Scientific / academic · Business / professional · General.
- **Lexical diversity is supporting-only:** max severity Medium, never the sole finding raised, capped Weak under 100 words.
- **Working branch:** `feature/v1.6-register-and-examples` (already checked out; the spec is already committed there).
- **Commit after every task.** Co-author trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## File Structure

- `skills/ai-pattern-detector/SKILL.md` — MODIFY. Adds Step 0 (register), a Step 1 metrics row, a Register line in the output template, a register-keyed "What not to flag" sub-list, a rewriting-rule pointer to examples.md, two new sources, and an intro-paragraph source mention.
- `skills/ai-pattern-detector/references/patterns.md` — MODIFY. Adds the nominalization tell to §9; folds in any genuinely-new pattern found in the Wikipedia refresh.
- `skills/ai-pattern-detector/references/examples.md` — CREATE. ~16 worked Before → After → Changes transformations, one-plus per register.
- `.claude-plugin/plugin.json` — MODIFY. Version bump.
- `skills/ai-pattern-detector/CHANGELOG.md` — MODIFY. New 1.6 entry.
- `README.md` — MODIFY. Feature description, structure block, "what it won't flag" note.

Task order is dependency-aware: register (T1) and the metric (T2) are independent SKILL.md edits; the nominalization tell (T3) and Wikipedia refresh (T4) both touch patterns.md and run back-to-back; examples.md (T5) is created and wired in; sources (T6) reflect the T4 refresh outcome; packaging + README (T7) come last so they describe the finished state.

---

### Task 1: Register-awareness in SKILL.md

**Files:**
- Modify: `skills/ai-pattern-detector/SKILL.md` (insert Step 0 before "## Step 1"; add a Register line in the Output format block; append a register sub-list to "What not to flag")

**Interfaces:**
- Produces: a "Step 0 — identify register" section; the seven register names; a `**Register:**` output line consumed by Task 7's README description; register calibration rules referenced by Task 3's nominalization tell ("drops to Weak in scientific register").

- [ ] **Step 1: Insert the Step 0 section**

In `SKILL.md`, immediately before the line `## Step 1 — quantified pre-scan`, insert:

```markdown
## Step 0 — identify register

Before measuring anything, infer the text's **register** — the kind of writing it is. Register decides *what counts as a tell*: a feature-adjective stack is native to a product description but a tell in an essay; short punchy lines are native to a social post but a metronome tell in a report. Register never changes the AI-feel score formula — only which findings you raise and how heavily each weighs.

Infer exactly one register from these signals:

| Register | Inferring signals |
|---|---|
| **Social post** (LinkedIn / X / Instagram) | short lines, one-thought-per-line breaks, hook opener, hashtags, emoji, @-mentions or handles |
| **Marketing / landing page** | headlines, benefit framing, CTAs, title-case section heads, feature blocks |
| **Product description / e-commerce** | feature/spec listing, "whether you're…", short benefit blurbs, bullet specs |
| **Blog / editorial / newsletter** | long-form prose, personal anecdote, section structure |
| **Scientific / academic** | citations, methods/passive constructions, hedged claims, domain terminology, "we" for own work, IMRaD structure, figures/tables |
| **Business / professional** | reports, memos, formal emails, corporate register |
| **General** | none of the above dominates (default) |

Rules:

- **Auto-infer; ask only when genuinely ambiguous.** Social, marketing, and product copy overlap — when a text sits between two, pick the closest and say which in the Register line. Ask the user only when you can't even pick a closest register.
- A short paragraph with no clear signals is **general** — don't ask.
- Report the inferred register in the output (see Output format) and apply its calibration from "What not to flag." Register never changes the score.

```

- [ ] **Step 2: Add the Register line to the output template**

In the Output format code block, find the `**Metrics:**` line and insert a new line directly **above** it:

```markdown
**Register:** <inferred register> — <one clause on how it calibrated the scan>
```

- [ ] **Step 3: Append the register-keyed sub-list to "What not to flag"**

At the end of the "## What not to flag" section (after the existing bullets, before the closing "When uncertain…" sentence if present — otherwise at the end of the list), insert:

```markdown

**Register-specific calibration** (from Step 0 — apply the row matching the inferred register):

- **Social post** — don't flag: short punchy lines (not a metronome tell — see the sentence-rhythm note in patterns.md §8), contractions, hashtags, emoji, one-line paragraphs. *Weighs heavier here:* engagement bait (§11), formulaic fragmentation (§8), fake-depth formulas (§3) — the dominant AI social tells.
- **Marketing / landing page** — don't flag: benefit language, a CTA, title-case headlines, some superlatives. *Still flag:* antithesis ("not just X — it's Y"), stacked buzzwords, "In today's world" openers, em-dash overuse, tricolon abuse.
- **Product description / e-commerce** — don't flag: feature-adjective stacking ("durable, lightweight, waterproof"), spec bullets, second-person "you". *Still flag:* AI vocab (elevate, seamless, game-changer), "whether you're X or Y", vague benefit inflation, "not just X — it's Y".
- **Blog / editorial / newsletter** — don't flag: "you", contractions, direct address, one earned fragment/punchline. *Weighs heavier here:* narrator-from-a-distance, engagement bait, uncontracted forms.
- **Scientific / academic** — don't flag: domain terminology, passive voice in methods, "we" for own work, conventional attribution ("the study found"), hedged claims ("may suggest"), standard field nominalization. *Still flag:* business-buzzword & AI-vocab leakage (leverage, landscape, delve, tapestry, nuanced), fake-depth formulas, em-dash overuse.
- **Business / professional** — don't flag: some domain jargon, formal tone. *Still flag:* stacked buzzwords, antithesis, closer clichés, both-sides-itis.
- **General** — the default calibration already described above; no register-specific relaxation.
```

- [ ] **Step 4: Verify the edits**

Run: `grep -n "Step 0 — identify register\|Register:\|Register-specific calibration" skills/ai-pattern-detector/SKILL.md`
Expected: three matches — the Step 0 heading, the output-template Register line, and the what-not-to-flag sub-heading.

Then Read the Step 0 section and confirm: seven registers present and named exactly per Global Constraints; the "never changes the score" clause appears; the ask-only-when-ambiguous rule is present.

- [ ] **Step 5: Commit**

```bash
git add skills/ai-pattern-detector/SKILL.md
git commit -m "Add Step 0 register identification and register-keyed calibration"
```

---

### Task 2: Lexical-diversity metric in SKILL.md

**Files:**
- Modify: `skills/ai-pattern-detector/SKILL.md` (add a row to the Step 1 metrics table; add a note in the "Notes on computing" paragraph)

**Interfaces:**
- Consumes: the existing Step 1 metrics table and its under-100-words cap.
- Produces: a "Lexical diversity / repetition" metric row referenced by Task 6 (source) and Task 7 (CHANGELOG/README).

- [ ] **Step 1: Add the metric row**

In the Step 1 metrics table, add this row directly after the `| Paragraph uniformity | … |` row (the last row of the table):

```markdown
| Lexical diversity / repetition | unique content words ÷ total content words over a ~150–250-word window (moving-average TTR, to control for length); also note any content word repeated 4+ times that isn't the topic term | TTR < ~0.40 over the window = **Medium** (corroborating only — never conclusive alone). Under-100-words cap applies (→ Weak). |
```

- [ ] **Step 2: Add the supporting-only note**

At the end of the "Notes on computing:" paragraph (right after the sentence beginning "Each metric over threshold counts as a finding…"), append:

```markdown
 **Lexical diversity is a *supporting* signal** (Terčon & Dobrovoljc 2025 survey: AI text runs lower-diversity than human writing): low diversity corroborates other findings but must never move the score on its own — never raise it above Medium, and don't raise it at all if it's the only thing you'd flag.
```

- [ ] **Step 3: Verify**

Run: `grep -n "Lexical diversity" skills/ai-pattern-detector/SKILL.md`
Expected: two matches — the table row and the supporting-only note.

Read both and confirm the "supporting-only / never sole finding / Medium max / Weak under 100 words" constraints all appear (Global Constraints).

- [ ] **Step 4: Commit**

```bash
git add skills/ai-pattern-detector/SKILL.md
git commit -m "Add lexical-diversity supporting metric to pre-scan"
```

---

### Task 3: Nominalization tell in patterns.md §9

**Files:**
- Modify: `skills/ai-pattern-detector/references/patterns.md` (add one bullet to "## 9. Voice and content tells")

**Interfaces:**
- Consumes: the register names and the "scientific register drops it to Weak" rule from Task 1.
- Produces: the nominalization tell, referenced by Task 5 example #15 and Task 7 (CHANGELOG).

- [ ] **Step 1: Add the nominalization bullet**

In `patterns.md`, at the end of the "## 9. Voice and content tells" section (after the "Agentless passive" bullet), add:

```markdown
- **Nominalization / noun-heavy register:** AI defaults to a formal, noun-stacked register — nominalizing verbs and adjectives ("the implementation of", "a reduction in", "the utilization of") and stacking determiners + prepositions ("the analysis of the impact of the changes on the system"). Cross-study surveys find AI text runs noun-, determiner-, and preposition-heavy and adjective/adverb-*light* vs. human writing. **Calibration:** do NOT read sparse adjectives/adverbs as a human signal — adjective-light *and* noun-heavy is the tell. This is not a contradiction of §1 (which flags specific AI-preferred adjectives when they appear); this flags the overall nominalized shape. Medium; drops to Weak in scientific/academic register where some nominalization is conventional (see "What not to flag" in SKILL.md). **Rewrite:** un-nominalize ("the utilization of X" → "using X") and name a verb + actor.
```

- [ ] **Step 2: Verify**

Run: `grep -n "Nominalization" skills/ai-pattern-detector/references/patterns.md`
Expected: one match, inside §9.

Confirm the category count is unchanged: run `grep -n "^## " skills/ai-pattern-detector/references/patterns.md` and verify it still lists sections 1 through 11 (no new top-level category).

- [ ] **Step 3: Commit**

```bash
git add skills/ai-pattern-detector/references/patterns.md
git commit -m "Add nominalization / noun-heavy register tell to patterns.md §9"
```

---

### Task 4: Wikipedia source refresh

**Files:**
- Modify: `skills/ai-pattern-detector/references/patterns.md` (only if the refresh surfaces a genuinely new pattern)

**Interfaces:**
- Produces: a verified-current or updated Wikipedia citation state, consumed by Task 6 (Sources).

- [ ] **Step 1: Fetch the live page**

Fetch `https://en.wikipedia.org/wiki/Wikipedia:Signs_of_AI_writing`. Extract its current list of patterns/tells (vocabulary, constructions, punctuation, formatting, structure).

- [ ] **Step 2: Diff against patterns.md**

Compare the fetched patterns against the eleven categories in `patterns.md`. Produce a short written list of any pattern present on the live page but NOT already covered anywhere in `patterns.md`.

- [ ] **Step 3: Fold in genuinely-new patterns (only if any)**

For each genuinely-new pattern, add a bullet to the most appropriate existing section of `patterns.md` (do not create a new top-level category — keep the count at 11). If the diff is empty, make no edit to `patterns.md` and note that for Task 6.

- [ ] **Step 4: Verify**

If patterns.md was edited: run `git diff skills/ai-pattern-detector/references/patterns.md` and confirm each added bullet is genuinely absent elsewhere (grep the key phrase to be sure it's not a duplicate). Confirm `grep -n "^## " patterns.md` still shows 11 categories.
If patterns.md was not edited: state "Wikipedia refresh found no new patterns; citation re-verified" so Task 6 records the refresh accurately.

- [ ] **Step 5: Commit (only if patterns.md changed)**

```bash
git add skills/ai-pattern-detector/references/patterns.md
git commit -m "Fold new patterns from Wikipedia Signs of AI writing refresh"
```

If no change, skip the commit and carry the "no new patterns" note to Task 6.

---

### Task 5: Create examples.md and wire it into SKILL.md

**Files:**
- Create: `skills/ai-pattern-detector/references/examples.md`
- Modify: `skills/ai-pattern-detector/SKILL.md` (add Rewriting rule 9; add a pointer in the Pattern deep-dive section)

**Interfaces:**
- Consumes: the pattern §-numbers from patterns.md and the register names from Task 1.
- Produces: `references/examples.md`, referenced by Task 7 (README structure block, CHANGELOG).

- [ ] **Step 1: Create examples.md with header and worked examples**

Create `skills/ai-pattern-detector/references/examples.md`. Start with this exact header, then the four fully-worked reference examples below, then complete the remaining twelve following the same format and the seed table in Step 2.

```markdown
# Worked Rewrite Examples

Companion to [SKILL.md](../SKILL.md) and [patterns.md](patterns.md). Each entry shows a real AI tell transformed: the pattern (cross-referenced to a patterns.md section), a Before, an After, and the specific Changes.

**When to read this:** during the rewrite / deep-dive phase, to model before → after transformations for a pattern you're fixing. Do NOT load this during Step 0–2 (register, pre-scan, pattern scan) — it is a rewrite aid, not a detection input.

Each "After" obeys the skill's rewriting rules: specific over abstract, cut over substitute, no AI tell swapped for another, and register-appropriate.

---

### §1 Telltale vocabulary — business / professional
**Before:** "We will leverage our cutting-edge platform to deliver a holistic, results-oriented solution that empowers stakeholders across the organization."
**After:** "Our platform gives every team the same live dashboard, so decisions stop waiting on a weekly report."
**Changes:** Cut "leverage", "cutting-edge", "holistic", "results-oriented", "empowers", "stakeholders". Replaced the abstract benefit with the concrete mechanism (a shared live dashboard) and the concrete payoff (no weekly-report lag).

### §3 False agency — scientific / academic
**Before:** "The data tells us that intervention timing drives the observed variance in recovery outcomes."
**After:** "We found that patients treated within 48 hours recovered faster; timing, not dose, explained most of the variance."
**Changes:** Named the actor ("we found") instead of letting "the data" speak. Added the specific finding (48-hour window) and the specific contrast (timing vs. dose) the vague sentence hid.

### §8 Formulaic fragmentation — social post
**Before:** "Most founders obsess over growth. But retention is what actually matters. Let that sink in."
**After:** "Most founders obsess over growth. We ignored retention for a year and churn quietly ate every new signup."
**Changes:** Cut the "Let that sink in." manufactured-profundity fragment and the empty "what actually matters" declarative. Replaced with a first-person, specific consequence (a year of ignored retention, churn eating signups).

### §3 "Whether you're X or Y" + benefit inflation — product description
**Before:** "Whether you're a busy professional or a weekend warrior, our revolutionary bottle elevates your hydration experience to the next level."
**After:** "Holds 1L, keeps water cold for 24 hours, and fits a bike cage. Stainless steel, dishwasher-safe."
**Changes:** Cut the "whether you're X or Y" inclusive frame, "revolutionary", "elevates", "hydration experience", "next level". Replaced with specs a buyer actually decides on (capacity, cold-retention, fit, material, care).

```

- [ ] **Step 2: Complete the remaining twelve examples**

Add twelve more entries in the same `### §N Pattern — register` / Before / After / Changes format, one per row below. Each row names the pattern (with patterns.md section), the register to write it in, and the trigger phrase the Before must contain. Write each After per the rewriting rules.

| # | Heading | Trigger phrase the Before must contain |
|---|---|---|
| 5 | §2 Stock opener — marketing / landing page | "In today's fast-paced world" |
| 6 | §3 Antithesis — product description | "not just a bag — it's a statement" |
| 7 | §3 Stacked-antithesis fake-depth formula — social post | "It's not talent. It's not luck. It's consistency." |
| 8 | §4 Hedge / throat-clearing — scientific / academic | "It's worth noting that" |
| 9 | §5 Meta-commentary — blog / editorial | "In this section, we'll explore" |
| 10 | §6 Closer — general | "In conclusion" |
| 11 | §8 Em-dash overuse — marketing / landing page | three em-dashes across two sentences |
| 12 | §8 Sentence-length uniformity — general | 5+ consecutive sentences of 18–22 words |
| 13 | §9 Vague declarative — scientific / academic | "The implications are significant" |
| 14 | §9 Narrator-from-a-distance — blog / editorial | "Nobody designed this" |
| 15 | §9 Nominalization — scientific / academic | "the utilization of" or "the implementation of" |
| 16 | §11 Engagement bait — social post | "Agree? Comment YES if this resonates" |

(That yields 16 total entries: examples 1–4 from Step 1 plus these 12. Renumber sequentially 1–16 in the file if you prefer; the numbering is cosmetic — coverage is what matters.)

- [ ] **Step 3: Add Rewriting rule 9 in SKILL.md**

In `SKILL.md`, at the end of the "## Rewriting rules" numbered list, add:

```markdown
9. **Consult the examples library.** For a pattern you're rewriting, see [references/examples.md](references/examples.md) for worked before → after transformations across registers. Read it during the rewrite / deep-dive phase only — never load it during the pre-scan.
```

- [ ] **Step 4: Add a deep-dive pointer in SKILL.md**

In the "## Pattern deep-dive" section of the Output format, in the "How to fix it" bullet (the one instructing 2-3 strategies with before → after examples), append this sentence to that bullet's text:

```markdown
 (See [references/examples.md](references/examples.md) for worked transformations of this pattern to model the before → after shape.)
```

- [ ] **Step 5: Verify**

Run: `grep -c "^### §" skills/ai-pattern-detector/references/examples.md`
Expected: `16`.

Run: `grep -n "examples.md" skills/ai-pattern-detector/SKILL.md`
Expected: at least two matches (Rewriting rule 9 and the deep-dive pointer).

Read three entries covering different registers (e.g., a social, a scientific, a product one) and confirm each After is specific, cuts rather than swaps tells, and fits its register.

- [ ] **Step 6: Commit**

```bash
git add skills/ai-pattern-detector/references/examples.md skills/ai-pattern-detector/SKILL.md
git commit -m "Add references/examples.md worked-rewrite library and wire into SKILL.md"
```

---

### Task 6: New sources in SKILL.md

**Files:**
- Modify: `skills/ai-pattern-detector/SKILL.md` (append two citations to "## Sources"; refresh the Wikipedia line per Task 4; extend the intro-paragraph source sentence)

**Interfaces:**
- Consumes: the Task 4 refresh outcome (new patterns folded in, or "no new patterns").

- [ ] **Step 1: Append the two new citations**

At the end of the "## Sources" bullet list, add:

```markdown
- Terčon & Dobrovoljc (2025), *Linguistic Characteristics of AI-Generated Text: A Survey* (arXiv:2510.05136) — cross-study synthesis: AI text runs noun/determiner/adposition-heavy, adjective/adverb-light, with lower lexical diversity and a smaller vocabulary range
- *AI-generated text detection: A comprehensive review of methods, datasets, and applications*, Computer Science Review (2025) (ScienceDirect S1574013725000693) — survey of detection methods, datasets, and applications; reinforces that the AI-feel score is a density-of-evidence measure, not an authorship verdict
```

- [ ] **Step 2: Refresh the Wikipedia citation line**

Find the existing bullet `- Wikipedia, *Signs of AI writing* — community-curated reference page (2026)` and update the year/note to reflect the Task 4 re-verification (e.g., `(community-curated reference page; re-verified 2026-07)`).

- [ ] **Step 3: Extend the intro-paragraph source sentence**

In the intro paragraph (the sentence beginning "The pattern catalog lives in … eleven categories drawn from …"), find "…and the Kobak et al. (2024) academic study on excess vocabulary in post-ChatGPT scientific abstracts." and replace the "and" so both new sources join the list:

Replace:
```
…Originality.ai's analyses, and the Kobak et al. (2024) academic study on excess vocabulary in post-ChatGPT scientific abstracts.
```
With:
```
…Originality.ai's analyses, the Kobak et al. (2024) academic study on excess vocabulary in post-ChatGPT scientific abstracts, the Terčon & Dobrovoljc (2025) survey of AI linguistic characteristics, and a 2025 Computer Science Review survey of detection methods.
```

- [ ] **Step 4: Verify**

Run: `grep -n "Terčon\|Computer Science Review\|re-verified" skills/ai-pattern-detector/SKILL.md`
Expected: matches for both new sources (in the intro sentence and the Sources list) and the refreshed Wikipedia note.

- [ ] **Step 5: Commit**

```bash
git add skills/ai-pattern-detector/SKILL.md
git commit -m "Add two 2025 sources and refresh Wikipedia citation"
```

---

### Task 7: Packaging and README

**Files:**
- Modify: `.claude-plugin/plugin.json` (version)
- Modify: `skills/ai-pattern-detector/CHANGELOG.md` (new 1.6 entry)
- Modify: `README.md` (feature description, structure block, "what it won't flag" note)

**Interfaces:**
- Consumes: everything from Tasks 1–6 (this task describes the finished state).

- [ ] **Step 1: Bump the version**

In `.claude-plugin/plugin.json`, change `"version": "1.5.0"` to `"version": "1.6.0"`. Leave the description unchanged.

- [ ] **Step 2: Add the CHANGELOG 1.6 entry**

At the top of `CHANGELOG.md` (directly under the `# Changelog` heading, above the `## 1.5` entry), insert:

```markdown
## 1.6 — 2026-07-24

Register-awareness and a worked-examples library, plus two new academic sources. Prompted by a comparison with the `deslop` skill (every-app/open-seo). Two of deslop's ideas were deliberately NOT adopted — a proactive "writing mode" (this stays a post-hoc detector) and a redundant "quick-checks" fast-pass (the quantified pre-scan metrics already do that job better).

### Added

- **Step 0 — register identification.** Every scan now infers one of seven registers (social post, marketing/landing page, product description, blog/editorial, scientific/academic, business/professional, general) from text signals, reports it in the output, and calibrates *what counts as a tell* accordingly — never the AI-feel score. Native conventions per register are no longer mis-flagged (feature-adjective stacks in product copy, short lines in social posts, methods-passive in papers), while register-specific AI tells weigh heavier where they dominate (engagement bait and formulaic fragmentation in social posts). Calibration rules live in an expanded "What not to flag."
- **Lexical-diversity / repetition metric (Step 1).** A supporting pre-scan metric — moving-average type-token ratio over a 150–250-word window; TTR below ~0.40 is a Medium tell. Corroborating only: it never inflates the score on its own and is capped Weak under 100 words. From the Terčon & Dobrovoljc (2025) finding that AI text has lower lexical diversity.
- **Nominalization / noun-heavy register tell (patterns.md §9).** Flags AI's formal, noun-stacked, nominalized shape, with a calibration note that adjective/adverb-sparse prose is NOT a human signal (adjective-light *and* noun-heavy is itself the tell) and that this does not contradict §1.
- **references/examples.md.** A library of worked Before → After → Changes transformations, one-plus per register, cross-referenced to the pattern catalog. Read on demand during the rewrite / deep-dive phase only, not during the pre-scan.
- **Two academic sources:** Terčon & Dobrovoljc (2025), *Linguistic Characteristics of AI-Generated Text: A Survey* (arXiv:2510.05136); and *AI-generated text detection: A comprehensive review of methods, datasets, and applications*, Computer Science Review (2025). Wikipedia *Signs of AI writing* re-verified.
```

- [ ] **Step 3: Update the README feature description**

In `README.md`, directly after the paragraph ending "…gives you concrete before → after rewrites for every finding." (the paragraph spanning the "eleven categories" sentence), insert a new paragraph:

```markdown

It first identifies the text's register — social post, landing page, product description, blog, scientific, business, or general — and calibrates what counts as a tell to match, so a product description's feature list or a scientific paper's methods-passive isn't mistaken for AI.
```

- [ ] **Step 4: Update the README structure block**

Replace the existing structure code block:

```
.claude-plugin/
  plugin.json                   # plugin manifest
skills/
  ai-pattern-detector/
    SKILL.md                    # workflow: metrics, scoring, output format
    references/patterns.md      # the eleven-category pattern catalog
    CHANGELOG.md
```

with:

```
.claude-plugin/
  plugin.json                   # plugin manifest
skills/
  ai-pattern-detector/
    SKILL.md                    # workflow: register, metrics, scoring, output format
    references/
      patterns.md               # the eleven-category pattern catalog
      examples.md               # worked before → after rewrites, by pattern
    CHANGELOG.md
```

- [ ] **Step 5: Add the register note to "What it won't flag"**

At the end of the "## What it won't flag" paragraph (after "…detectors are unreliable, and the skill says so."), append:

```markdown
 It also adapts to register: it won't flag a product description's feature-adjective stack or a scientific paper's methods-passive, which are native there.
```

- [ ] **Step 6: Verify**

Run: `grep -n '"version"' .claude-plugin/plugin.json`
Expected: `"version": "1.6.0"`.

Run: `grep -n "## 1.6" skills/ai-pattern-detector/CHANGELOG.md`
Expected: one match.

Run: `grep -n "examples.md\|register" README.md`
Expected: matches in the structure block, the new feature paragraph, and the "what it won't flag" note.

Confirm `README.md` still says "eleven" categories (unchanged — Global Constraints).

- [ ] **Step 7: Commit**

```bash
git add .claude-plugin/plugin.json skills/ai-pattern-detector/CHANGELOG.md README.md
git commit -m "Bump to v1.6.0: changelog entry and README updates"
```

---

## Final Verification (after all tasks)

- [ ] Run `git log --oneline feature/v1.6-register-and-examples` and confirm one commit per completed task (Task 4 may be a note-only no-commit).
- [ ] Run `python -c "import json; json.load(open('.claude-plugin/plugin.json'))"` (or `node -e "require('./.claude-plugin/plugin.json')"`) to confirm `plugin.json` still parses and shows version 1.6.0.
- [ ] Read `SKILL.md` end-to-end once: confirm the flow reads Step 0 → Step 1 → Step 2 → scoring → output → rewriting rules, with register threaded through and the score formula untouched.
- [ ] Sanity scan: mentally run the skill on a one-line product description ("Whether you're a pro or a beginner, our revolutionary tool elevates your workflow.") and confirm the instructions now (a) infer "product description", (b) still flag "revolutionary/elevates" and the "whether you're X or Y" frame, and (c) don't flag a plain feature list.
```
